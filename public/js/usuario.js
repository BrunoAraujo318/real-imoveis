var realImovel = realImovel || {};

realImovel.usuario = (function ($) {

    /**
	 * Inicializa o modulo.
	 */
    var iniciar = function () {
    }

    /**
     * Retorna api publica.
     */
    return {
        iniciar : iniciar,
    };

})(jQuery);
